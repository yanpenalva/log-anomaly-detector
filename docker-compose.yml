services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      DB_DRIVER: sqlite
      DB_SQLITE_PATH: /app/data/database.sqlite
    volumes:
      - sqlite-data:/app/data
volumes:
  sqlite-data:
